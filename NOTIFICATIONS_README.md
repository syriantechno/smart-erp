# 📢 نظام الإشعارات الموحد

## 🎯 **نظرة عامة**
نظام إشعارات متقدم يتيح للمستخدمين تلقي الإشعارات في الوقت الفعلي مع إمكانيات إدارة شاملة.

## ✨ **الميزات**

### **🔔 جرس الإشعارات**
- عرض عدد الإشعارات غير المقروءة
- dropdown تفاعلي يعرض الإشعارات الأخيرة
- تحديث تلقائي كل 30 ثانية

### **📋 إدارة الإشعارات**
- **قراءة فردية:** تحديد إشعار كمقروء
- **قراءة الكل:** تحديد جميع الإشعارات كمقروءة
- **حذف فردي:** حذف إشعار محدد
- **حذف الكل:** حذف جميع الإشعارات
- **الانتقال:** النقر على الإشعار للانتقال للصفحة المعنية

### **🎨 أنواع الإشعارات**
- **Info** 🔵 - معلومات عامة
- **Success** 🟢 - عمليات ناجحة
- **Warning** 🟡 - تحذيرات
- **Error** 🔴 - أخطاء

---

## 🚀 **كيفية الاستخدام**

### **1. عرض الإشعارات (تلقائي)**
```blade
<x-notifications.dropdown :unread-count="count" />
```

### **2. إرسال إشعار لجميع المستخدمين**
```php
use App\Http\Controllers\NotificationController;

NotificationController::sendToAllUsers(
    'New Position Added',           // العنوان
    'Position "Developer" created', // الرسالة
    'success',                      // النوع
    route('hr.positions.index'),    // رابط الإجراء
    'Plus'                          // الأيقونة
);
```

### **3. إرسال إشعار لمستخدم محدد**
```php
NotificationController::sendToUser(
    $userId,                        // ID المستخدم
    'Task Assigned',               // العنوان
    'You have a new task',         // الرسالة
    'info',                        // النوع
    route('tasks.show', $taskId),  // الرابط
    'ClipboardList'                // الأيقونة
);
```

### **4. إرسال إشعار لمستخدمين متعددين**
```php
NotificationController::sendToUsers(
    [$userId1, $userId2],         // مصفوفة IDs
    'Meeting Reminder',           // العنوان
    'Meeting starts in 30 mins',  // الرسالة
    'warning',                    // النوع
    route('meetings.show', $id),  // الرابط
    'Clock'                       // الأيقونة
);
```

---

## 📡 **API Endpoints**

| Method | Endpoint | Description |
|--------|----------|-------------|
| GET | `/notifications` | قائمة الإشعارات |
| GET | `/notifications/unread-count` | عدد غير المقروءة |
| GET | `/notifications/recent` | الإشعارات الأخيرة |
| PATCH | `/notifications/{id}/read` | تحديد مقروءة |
| PATCH | `/notifications/mark-all-read` | قراءة الكل |
| DELETE | `/notifications/{id}` | حذف إشعار |
| DELETE | `/notifications/delete-all` | حذف الكل |
| POST | `/notifications/create` | إنشاء إشعار |
| POST | `/notifications/send-to-users` | إرسال لمتعددين |

---

## 🎨 **تخصيص الأيقونات**

### **أيقونات Heroicons المدعومة:**
- `CheckCircle` - نجاح
- `ExclamationCircle` - خطأ
- `ExclamationTriangle` - تحذير
- `InformationCircle` - معلومات
- `UserPlus` - مستخدم جديد
- `Plus` - إضافة
- `Trash` - حذف
- `Pencil` - تعديل

---

## 🔧 **الأحداث التلقائية**

### **إشعارات تُرسل تلقائياً:**
- ✅ **إنشاء بوزيشن جديد**
- ✅ **إنشاء ديبارتمنت جديد**
- ✅ **إمكانية إضافة المزيد**

### **إضافة إشعار لأي حدث:**
```php
// في أي Controller
public function someAction()
{
    // ... الكود

    NotificationController::sendToAllUsers(
        'Custom Event',
        'Something happened',
        'info'
    );

    // ... باقي الكود
}
```

---

## 📱 **التصميم المتجاوب**

- ✅ **موبايل:** جرس بسيط مع badge
- ✅ **تابلت:** dropdown متوسط
- ✅ **ديسكتوب:** dropdown كامل
- ✅ **جميع المتصفحات:** مدعوم بالكامل

---

## ⚡ **الأداء**

- **تحديث تلقائي:** كل 30 ثانية
- **تحميل تدريجي:** 20 إشعار لكل صفحة
- **فهرسة قاعدة البيانات:** محسنة للسرعة
- **Lazy Loading:** تحميل عند الحاجة

---

## 🛠️ **استكشاف الأخطاء**

### **الإشعارات لا تظهر:**
```javascript
// تحقق من Console
console.log('Unread count:', unreadCount);
```

### **الـ API لا يعمل:**
```bash
# تحقق من Routes
php artisan route:list | grep notifications
```

### **الجرس لا يتحدث:**
```javascript
// في Browser Console
document.querySelector('[x-data*="notificationDropdown"]')
```

---

## 📚 **الملفات المعنية**

```
app/
├── Models/Notification.php
└── Http/Controllers/NotificationController.php

database/migrations/
└── create_notifications_table.php

resources/views/
├── components/notifications/dropdown.blade.php
├── notifications/index.blade.php
└── components/themes/icewall/top-bar/index.blade.php

routes/web.php (تحديث)
```

---

## 🎉 **جاهز للاستخدام!**

نظام الإشعارات مكتمل بالكامل وجاهز للاستخدام في الإنتاج! 🚀
